# Text Analytics

## build

    $ ./autogen.sh
    $ cd build
    $ ../configure
    $ make
    $ make install

## testing

Test programs are supplied for each function defined in `analytics.h` and will be compiled in the `bin` sub-directory of the build directory.
